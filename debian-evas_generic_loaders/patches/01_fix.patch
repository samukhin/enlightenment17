diff --git a/src/bin/pdf/main.cpp b/src/bin/pdf/main.cpp
index b1fe1f2..7e03f4f 100644
--- a/src/bin/pdf/main.cpp
+++ b/src/bin/pdf/main.cpp
@@ -5,6 +5,8 @@
 #include <sys/mman.h>
 #include <fcntl.h>
 
+#include <memory>
+
 #include <GlobalParams.h>
 #include <PDFDoc.h>
 #include <ErrorCodes.h>
@@ -52,16 +54,12 @@ Eina_Bool poppler_init(const char *file, int page_nbr, int size_w, int size_h)
    if (page_nbr < 0)
      return EINA_FALSE;
 
-   if (!(globalParams = new GlobalParams()))
-     return EINA_FALSE;
+    globalParams = std::make_unique<GlobalParams>();
 
    if (!eina_init())
      goto del_global_param;
 
-   if (globalParams->getAntialias())
-     globalParams->setAntialias((char *)"yes");
-   if (globalParams->getVectorAntialias())
-     globalParams->setVectorAntialias((char *)"yes");
+    // antialias is enabled by default in poppler
 
    pdfdoc = new PDFDoc(new GooString(file), NULL);
    if (!pdfdoc)
@@ -125,19 +123,19 @@ Eina_Bool poppler_init(const char *file, int page_nbr, int size_w, int size_h)
 
    return EINA_TRUE;
 
- del_pdfdoc:
-   delete pdfdoc;
- del_global_param:
-   delete globalParams;
+  del_pdfdoc:
+    delete pdfdoc;
+  del_global_param:
+    // globalParams is unique_ptr, no need to delete
 
-   return EINA_FALSE;
+    return EINA_FALSE;
 }
 
 void poppler_shutdown()
 {
-   delete pdfdoc;
-   eina_shutdown();
-   delete globalParams;
+    delete pdfdoc;
+    eina_shutdown();
+    // globalParams is unique_ptr, no need to delete
 }
 
 void poppler_load_image(int size_w, int size_h)
@@ -153,7 +151,7 @@ void poppler_load_image(int size_w, int size_h)
    white[2] = 255;
    white[3] = 255;
 
-   output_dev = new SplashOutputDev(splashModeXBGR8, 4, gFalse, white);
+    output_dev = new SplashOutputDev(splashModeXBGR8, 4, false, white);
    if (!output_dev)
      return;
 
